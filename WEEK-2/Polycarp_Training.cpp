#include<bits/stdc++.h>
using namespace std;
int main(){
ios::sync_with_stdio(false);
cin.tie(nullptr);
    int n;
    cin>>n;
    vector<int> contest(n);

    for(int i =1; i<=n; i++)
    {
        cin>>contest[i]<<" ";
    }

   return 0;
}